<%- include ('../partials/header') %>
<%- include ('../partials/menu') %>
<div class="col"><%- include ('../partials/message') %></div>

<main>
    <center><p style="font-family: impact"><h1><b>Welcome to PowderPuff!</b></h1></p></center>
    <center>
    <h3>--------------------------------------------------------------------------------------------------------------------------------------------</h3>
    <p style="font-family: georgia">You can scroll down and look at all the different resorts in Colorado. You can view more information on each resort by using the "Visit Informational Page" button. You can make a new account on our website if you <a href = "register">Click here</a>.</p>
    <p style="font-family: georgia">If you already have an account, you can login if you <a href = "login">Click here</a>. Once you login, you can add your own reviews to any of the resorts based on your experience and give a rating as well!</p>
    </center>

    <div class="container">
        <div class="card-deck mt-3">
        <% if(resorts.length !== 0){ %>
            <% var i = 0 %>
            <% resorts.forEach(resort =>{ %>
                <% if (i % 3 === 0 || i + 1 === 1){ %>
                    <div class="row" >
                <%}%>
                <div class="col-sm-4 card">
                    <div class="card-block">
                        <img src="<%= resort.image %>" alt="Image" height="200" width="325">
                        <div class="container text-center">
                            <p></p>
                            <h5><b><%= resort.name %></b></h5>
                            <p>Location: <%= resort.location %></p>
                        </div>
                    </div>
                    <div class="card-footer">
                        <form method="post" action="/resort">
                            <input type = "hidden" name = "resort_id" value = "<%- resort.resort_id %>">
                            <button>Visit Informational Page</button>
                        </form>
                    </div>
                </div>
                  <% i = i + 1 %>
                  <% if (i % 3 === 0 || i === resorts.length){ %>
                    </div>
                    <p>---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------</p>
                    <%}%>
            <% }); %>
        <% } %>
    </div>
    </div>
</main>

<%- include ('../partials/footer') %>